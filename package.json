{
  "name": "color-guessing-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "vite-build": "vite build",
    "preview": "vite preview",
    "tailwind-build": "npx tailwindcss -i ./css/input.css -o ./public/build.css --watch",
    "tailwind-minify-build": "npx tailwindcss -o ./public/build.css --minify",
    "script": "npm run dev & npm run tailwind-build",
    "build": "npm run tailwind-minify-build && npm run vite-build"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.9",
    "postcss": "^8.4.16",
    "tailwindcss": "^3.1.8",
    "vite": "^3.1.0"
  }
}
